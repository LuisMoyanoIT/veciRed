/* import { ComponentFixture, TestBed, waitForAsync } from '@angular/core/testing';
import { IonicModule } from '@ionic/angular';

import { AcuerdosComunidadComponent } from './acuerdos-comunidad.component';

describe('AcuerdosComunidadComponent', () => {
  let component: AcuerdosComunidadComponent;
  let fixture: ComponentFixture<AcuerdosComunidadComponent>;

  beforeEach(waitForAsync(() => {
    TestBed.configureTestingModule({
      declarations: [ AcuerdosComunidadComponent ],
      imports: [IonicModule.forRoot()]
    }).compileComponents();

    fixture = TestBed.createComponent(AcuerdosComunidadComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
}); */
