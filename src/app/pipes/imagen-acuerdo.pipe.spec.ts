import { ImagenAcuerdoPipe } from './imagen-acuerdo.pipe';

describe('ImagenAcuerdoPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenAcuerdoPipe();
    expect(pipe).toBeTruthy();
  });
});
