import { MostrarImagenPipe } from './mostrar-imagen.pipe';

describe('MostrarImagenPipe', () => {
  it('create an instance', () => {
    const pipe = new MostrarImagenPipe();
    expect(pipe).toBeTruthy();
  });
});
